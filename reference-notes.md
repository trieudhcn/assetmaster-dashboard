# Ghi nhận tham chiếu giao diện

- Ảnh tham chiếu thứ nhất hiển thị nhãn định danh dạng chữ hoa: `ASSET OPERATIONS`, có chấm tròn màu hổ phách ở bên trái.
- Ảnh tham chiếu thứ hai hiển thị nhãn định danh dạng chữ hoa: `ASSET REGISTRY`, có chấm tròn màu teal ở bên trái.
- Yêu cầu áp dụng cơ chế nhấp đúp để Admin có thể đổi nội dung các nhãn định danh tương tự trực tiếp trên giao diện.
