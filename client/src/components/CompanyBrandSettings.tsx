import { useEffect, useRef, useState } from "react";
import { Building2, CheckCircle2, Copy, ImageUp, LoaderCircle, Moon, Sun, Upload, X } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";

export type CompanyBrandInfo = { name: string; address: string; taxCode: string; phone: string; email: string; websiteTitle: string; logoUrl: string; brandColor: string; faviconUrl: string; loginBackgroundUrl: string; loginGreeting: string; loginBackgroundOverlay: "light" | "dark" };

export function CompanyBrandSettings({ companyInfo, onSave }: { companyInfo: CompanyBrandInfo; onSave: (next: CompanyBrandInfo) => void }) {
  const [draft, setDraft] = useState(companyInfo);
  const logoInputRef = useRef<HTMLInputElement>(null);
  const backgroundInputRef = useRef<HTMLInputElement>(null);
  const uploadLogo = trpc.company.uploadLogo.useMutation({ onError: (error) => toast.error(error.message || "Không thể tải logo.") });
  const uploadLoginBackground = trpc.company.uploadLoginBackground.useMutation({ onError: (error) => toast.error(error.message || "Không thể tải ảnh nền đăng nhập.") });
  useEffect(() => setDraft(companyInfo), [companyInfo]);
  const update = (key: keyof CompanyBrandInfo, value: string) => setDraft((current) => ({ ...current, [key]: value }));
  const readUpload = (file: File, onComplete: (dataUrl: string) => void) => { const reader = new FileReader(); reader.onload = () => onComplete(String(reader.result)); reader.onerror = () => toast.error("Không thể đọc tệp ảnh."); reader.readAsDataURL(file); };
  const chooseLogo = (file?: File) => {
    if (!file) return;
    if (!['image/png', 'image/jpeg', 'image/webp'].includes(file.type) || file.size > 2 * 1024 * 1024) { toast.error("Logo chỉ hỗ trợ PNG, JPG, WebP và tối đa 2 MB."); return; }
    readUpload(file, (dataUrl) => uploadLogo.mutate({ fileName: file.name, contentType: file.type as "image/png" | "image/jpeg" | "image/webp", dataUrl }, { onSuccess: ({ url }) => { update("logoUrl", url); toast.success("Đã tải logo. Hãy lưu cài đặt để áp dụng."); } }));
  };
  const chooseBackground = (file?: File) => {
    if (!file) return;
    if (!['image/png', 'image/jpeg', 'image/webp'].includes(file.type) || file.size > 5 * 1024 * 1024) { toast.error("Ảnh nền chỉ hỗ trợ PNG, JPG, WebP và tối đa 5 MB."); return; }
    readUpload(file, (dataUrl) => uploadLoginBackground.mutate({ fileName: file.name, contentType: file.type as "image/png" | "image/jpeg" | "image/webp", dataUrl }, { onSuccess: ({ url }) => { update("loginBackgroundUrl", url); toast.success("Đã tải ảnh nền. Hãy lưu cài đặt để áp dụng."); } }));
  };
  const save = () => { if (!draft.name.trim() || !draft.websiteTitle.trim()) { toast.error("Vui lòng nhập tên công ty và tiêu đề website."); return; } onSave({ ...draft, loginGreeting: draft.loginGreeting.trim() }); };
  const removeBackground = () => { update("loginBackgroundUrl", ""); toast.success("Đã gỡ ảnh nền. Hãy lưu cài đặt để áp dụng nền mặc định."); };
  const copyContact = async (value: string, label: string) => {
    const content = value.trim();
    if (!content) return;
    try {
      let copied = false;
      if (navigator.clipboard?.writeText) {
        try { await navigator.clipboard.writeText(content); copied = true; } catch { /* Use the document fallback below. */ }
      }
      if (!copied) {
        const textarea = document.createElement("textarea");
        textarea.value = content;
        textarea.setAttribute("readonly", "");
        textarea.style.position = "fixed";
        textarea.style.opacity = "0";
        document.body.appendChild(textarea);
        textarea.select();
        copied = document.execCommand("copy");
        textarea.remove();
      }
      if (!copied) throw new Error("Clipboard unavailable");
      toast.success(`Đã sao chép ${label}.`);
    } catch { toast.error(`Không thể sao chép ${label}.`); }
  };

  return <div className="min-h-screen bg-[#F4F7FB] px-4 py-7 sm:px-6 lg:px-9 lg:py-8"><div className="mx-auto max-w-[1100px]"><div className="mb-7"><div className="mb-2 flex items-center gap-2 text-[11px] font-bold uppercase tracking-[0.16em] text-[#0F8C8C]"><span className="h-1.5 w-1.5 rounded-full bg-[#F0A516]" />Thiết lập thương hiệu</div><h1 className="font-display text-[28px] font-extrabold tracking-[-0.045em] text-[#102A43] sm:text-[34px]">Cài đặt thương hiệu</h1><p className="mt-1.5 text-sm text-[#71869A]">Thiết lập nhận diện công ty, website và trải nghiệm đăng nhập.</p></div><section className="rounded-xl border border-[#DFE9F0] bg-white p-6 shadow-[0_8px_24px_rgba(16,42,67,.045)]"><div className="flex items-start gap-3 border-b border-[#E7EEF3] pb-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#E6F6F2] text-[#0F8C8C]"><Building2 size={19} /></div><div><h2 className="font-display text-base font-extrabold text-[#102A43]">Thông tin công ty</h2><p className="mt-1 text-xs text-[#8AA0B6]">Các thông tin này được dùng cho website, đăng nhập và biên bản bàn giao.</p></div></div><div className="mt-5 space-y-4"><div><label className="field-label">Tên công ty <span className="text-[#0F8C8C]">*</span></label><input value={draft.name} onChange={(event) => update("name", event.target.value)} className="field-input" /></div><div><label className="field-label">Tiêu đề website <span className="text-[#0F8C8C]">*</span></label><input value={draft.websiteTitle} onChange={(event) => update("websiteTitle", event.target.value)} placeholder="Hệ thống Quản lý Tài sản" className="field-input" /><p className="mt-1 text-[11px] text-[#8AA0B6]">Hiển thị trên tab trình duyệt và ở phần nhận diện đăng nhập.</p></div><div><label className="field-label">Logo công ty</label><div className="flex flex-col gap-3 rounded-xl border border-dashed border-[#9ADBD3] bg-[#F8FCFB] p-4 sm:flex-row sm:items-center"><div className="grid h-16 w-16 shrink-0 place-items-center overflow-hidden rounded-xl bg-[#102A43] text-white">{draft.logoUrl ? <img src={draft.logoUrl} alt="Logo công ty" className="h-full w-full object-contain" /> : <ImageUp size={22} />}</div><div className="min-w-0 flex-1"><div className="text-xs font-extrabold text-[#193B57]">PNG, JPG hoặc WebP · tối đa 2 MB</div><p className="mt-1 text-[11px] leading-5 text-[#71869A]">Logo sẽ xuất hiện trong các khu vực nhận diện của AssetMaster.</p><button disabled={uploadLogo.isPending} onClick={() => logoInputRef.current?.click()} className="mt-2 inline-flex items-center gap-2 rounded-lg border border-[#8BCDC6] bg-white px-3 py-2 text-xs font-bold text-[#087A6A] disabled:opacity-50">{uploadLogo.isPending ? <LoaderCircle size={14} className="animate-spin" /> : <Upload size={14} />}{uploadLogo.isPending ? "Đang tải..." : "Chọn logo"}</button><input ref={logoInputRef} type="file" accept="image/png,image/jpeg,image/webp" className="sr-only" onChange={(event) => { chooseLogo(event.target.files?.[0]); event.currentTarget.value = ""; }} /></div></div></div><div><label className="field-label">Mã số thuế</label><input value={draft.taxCode} onChange={(event) => update("taxCode", event.target.value)} className="field-input" /></div><div className="grid gap-4 sm:grid-cols-2"><div><label className="field-label">Số điện thoại</label><div className="relative"><input value={draft.phone} onChange={(event) => update("phone", event.target.value)} className="field-input pr-10" /><button type="button" disabled={!draft.phone.trim()} onClick={() => { void copyContact(draft.phone, "Số điện thoại"); }} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-md p-1.5 text-[#60758A] transition hover:bg-[#ECF8F7] hover:text-[#087A6A] disabled:cursor-not-allowed disabled:opacity-40" aria-label="Sao chép Số điện thoại" title="Sao chép Số điện thoại"><Copy size={15} /></button></div></div><div><label className="field-label">Email công ty</label><div className="relative"><input type="email" value={draft.email} onChange={(event) => update("email", event.target.value)} placeholder="contact@company.vn" className="field-input pr-10" /><button type="button" disabled={!draft.email.trim()} onClick={() => { void copyContact(draft.email, "Email công ty"); }} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-md p-1.5 text-[#60758A] transition hover:bg-[#ECF8F7] hover:text-[#087A6A] disabled:cursor-not-allowed disabled:opacity-40" aria-label="Sao chép Email công ty" title="Sao chép Email công ty"><Copy size={15} /></button></div></div></div><div><label className="field-label">Địa chỉ</label><textarea value={draft.address} onChange={(event) => update("address", event.target.value)} className="field-input min-h-[76px] resize-y" /></div></div><div className="mt-6 flex justify-end border-t border-[#E7EEF3] pt-4"><button onClick={save} className="flex items-center gap-2 rounded-lg bg-[#0F8C8C] px-4 py-2.5 text-xs font-bold text-white hover:bg-[#087A6A]"><CheckCircle2 size={15} />Lưu cài đặt thương hiệu</button></div></section><section className="mt-5 rounded-xl border border-[#DFE9F0] bg-white p-6 shadow-[0_8px_24px_rgba(16,42,67,.045)]"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#EAF3FF] text-[#2666A8]"><ImageUp size={19} /></div><div><h2 className="font-display text-base font-extrabold text-[#102A43]">Nhận diện màn hình đăng nhập</h2><p className="mt-1 text-xs text-[#8AA0B6]">Tùy chỉnh ảnh nền, lớp phủ và câu chào nổi bật trước khi người dùng đăng nhập.</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.18fr_.82fr]"><div className="overflow-hidden rounded-xl border border-dashed border-[#9ADBD3] bg-[#F8FCFB]"><div className="relative h-44 bg-[#102A43] bg-cover bg-center" style={draft.loginBackgroundUrl ? { backgroundImage: `${draft.loginBackgroundOverlay === "dark" ? "linear-gradient(90deg, rgba(16,42,67,.84), rgba(16,42,67,.52))" : "linear-gradient(90deg, rgba(244,247,251,.78), rgba(244,247,251,.28))"}, url(${draft.loginBackgroundUrl})` } : undefined}><div className={`absolute inset-x-4 bottom-4 text-xs font-bold ${draft.loginBackgroundOverlay === "dark" ? "text-white" : "text-[#102A43]"}`}>{draft.loginBackgroundUrl ? "Ảnh nền đăng nhập đang được chọn" : "Chưa có ảnh nền tùy chỉnh"}</div></div><div className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-xs font-extrabold text-[#193B57]">Ảnh nền đăng nhập</div><p className="mt-1 text-[11px] leading-5 text-[#71869A]">PNG, JPG hoặc WebP · tối đa 5 MB. Chọn lớp phủ phù hợp để chữ luôn rõ.</p></div><div className="flex shrink-0 gap-2"><button disabled={uploadLoginBackground.isPending} onClick={() => backgroundInputRef.current?.click()} className="inline-flex items-center justify-center gap-2 rounded-lg border border-[#8BCDC6] bg-white px-3 py-2 text-xs font-bold text-[#087A6A] disabled:opacity-50">{uploadLoginBackground.isPending ? <LoaderCircle size={14} className="animate-spin" /> : <Upload size={14} />}{uploadLoginBackground.isPending ? "Đang tải..." : "Tải ảnh nền"}</button>{draft.loginBackgroundUrl ? <button type="button" onClick={removeBackground} className="inline-flex items-center justify-center gap-1.5 rounded-lg border border-[#F2C7C7] bg-white px-3 py-2 text-xs font-bold text-[#B44545]"><X size={14} />Gỡ ảnh nền</button> : null}</div><input ref={backgroundInputRef} type="file" accept="image/png,image/jpeg,image/webp" className="sr-only" onChange={(event) => { chooseBackground(event.target.files?.[0]); event.currentTarget.value = ""; }} /></div></div><div className="space-y-4"><div><label className="field-label">Lớp phủ ảnh nền</label><div className="grid grid-cols-2 gap-2"><button type="button" onClick={() => update("loginBackgroundOverlay", "light")} className={`flex items-center justify-center gap-2 rounded-lg border px-3 py-2.5 text-xs font-bold transition ${draft.loginBackgroundOverlay === "light" ? "border-[#8BCDC6] bg-[#ECF8F7] text-[#087A6A]" : "border-[#DDE7F0] bg-white text-[#60758A]"}`}><Sun size={15} />Sáng</button><button type="button" onClick={() => update("loginBackgroundOverlay", "dark")} className={`flex items-center justify-center gap-2 rounded-lg border px-3 py-2.5 text-xs font-bold transition ${draft.loginBackgroundOverlay === "dark" ? "border-[#294F6A] bg-[#173A56] text-white" : "border-[#DDE7F0] bg-white text-[#60758A]"}`}><Moon size={15} />Tối</button></div><p className="mt-1 text-[11px] leading-5 text-[#71869A]">Lớp phủ được áp dụng trên ảnh nền để đảm bảo văn bản dễ đọc.</p></div><div><label className="field-label">Câu chào màn hình đăng nhập</label><textarea value={draft.loginGreeting} maxLength={300} onChange={(event) => update("loginGreeting", event.target.value)} placeholder="Quản lý tài sản, theo đúng vai trò của bạn." className="field-input min-h-[132px] resize-y" /><div className="mt-1 flex justify-between text-[11px] text-[#8AA0B6]"><span>Hiển thị ở phần nổi bật của màn hình đăng nhập.</span><span>{draft.loginGreeting.length}/300</span></div></div></div></div></section></div></div>;
}
