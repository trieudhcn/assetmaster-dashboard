import { useEffect, useMemo, useRef, useState } from "react";
import { createRoot } from "react-dom/client";
import * as XLSX from "xlsx";
import { Eye, FileCode2, FileImage, FileText, Link2, Loader2, Paperclip, Pencil, Plus, ReceiptText, Search, X } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { SearchableSelect } from "@/components/SearchableSelect";
import { CurrencyInput } from "@/components/CurrencyInput";
import { DatePickerField } from "@/components/DatePickerField";
import { InvoiceLineOperationsPanel } from "@/components/InvoiceLineOperationsPanel";

type InvoiceStatus = "draft" | "issued" | "adjusted" | "replaced" | "cancelled";
type InvoiceType = "vat" | "electronic" | "retail" | "adjustment" | "replacement" | "other";
type InvoiceForm = { invoiceNumber: string; invoiceSeries: string; invoiceTemplate: string; invoiceType: InvoiceType; status: InvoiceStatus; vendorId: string; purchaseContractId: string; issuedAt: string; receivedAt: string; subtotalAmount: string; taxAmount: string; totalAmount: string; note: string };
type InvoiceLineForm = { id: string; itemType: "asset" | "supply" | "service" | "other"; itemCode: string; itemName: string; quantity: string; unit: string; unitPrice: string; taxRate: string; note: string };
type QueuedDocument = { id: string; file: File; documentType: "invoice_pdf" | "invoice_xml" | "scan" | "delivery_note" | "adjustment" | "other" };

const emptyForm: InvoiceForm = { invoiceNumber: "", invoiceSeries: "", invoiceTemplate: "", invoiceType: "vat", status: "issued", vendorId: "", purchaseContractId: "", issuedAt: new Date().toISOString().slice(0, 10), receivedAt: "", subtotalAmount: "", taxAmount: "", totalAmount: "", note: "" };
const invoiceStatuses: Record<string, { label: string; tone: string }> = { draft: { label: "Nháp", tone: "bg-[#EAF3FF] text-[#2666A8]" }, issued: { label: "Đã phát hành", tone: "bg-[#E6F6F2] text-[#087A6A]" }, adjusted: { label: "Điều chỉnh", tone: "bg-[#FFF5DC] text-[#A86B00]" }, replaced: { label: "Thay thế", tone: "bg-[#F1ECFF] text-[#7554B8]" }, cancelled: { label: "Đã hủy", tone: "bg-[#FDEDEE] text-[#B44545]" } };
const invoiceTypes: Record<string, string> = { vat: "Hóa đơn GTGT", electronic: "Hóa đơn điện tử", retail: "Hóa đơn bán lẻ", adjustment: "Hóa đơn điều chỉnh", replacement: "Hóa đơn thay thế", other: "Khác" };
const documentTypes: Array<{ value: QueuedDocument["documentType"]; label: string }> = [{ value: "invoice_pdf", label: "Bản PDF" }, { value: "invoice_xml", label: "Dữ liệu XML" }, { value: "scan", label: "Ảnh scan" }, { value: "delivery_note", label: "Phiếu giao hàng" }, { value: "adjustment", label: "Chứng từ điều chỉnh" }, { value: "other", label: "Khác" }];
const acceptedInvoiceTypes = ["application/pdf", "application/xml", "text/xml", "image/png", "image/jpeg"] as const;

function toDateInput(value: Date | string | number | null | undefined) { if (!value) return ""; const date = new Date(value); return Number.isFinite(date.getTime()) ? date.toISOString().slice(0, 10) : ""; }
function toTimestamp(value: string) { return value ? new Date(`${value}T00:00:00`).getTime() : null; }
function money(value: string | number | null | undefined) { return value === null || value === undefined || value === "" ? "—" : `${Number(value).toLocaleString("vi-VN", { maximumFractionDigits: 0 })} VNĐ`; }
function dateLabel(value: Date | string | number | null | undefined) { return value ? new Intl.DateTimeFormat("vi-VN", { dateStyle: "medium" }).format(new Date(value)) : "—"; }
function readFileAsDataUrl(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => typeof reader.result === "string" ? resolve(reader.result) : reject(new Error("Không thể đọc tệp.")); reader.onerror = () => reject(new Error("Không thể đọc tệp.")); reader.readAsDataURL(file); }); }
function normalizedContentType(file: File) { return file.type === "text/xml" || (!file.type && file.name.toLowerCase().endsWith(".xml")) ? "application/xml" : file.type; }
function newLine(): InvoiceLineForm { return { id: crypto.randomUUID(), itemType: "asset", itemCode: "", itemName: "", quantity: "1", unit: "cái", unitPrice: "", taxRate: "0", note: "" }; }

export function PurchaseInvoiceManagementView({ sharedQuery = "" }: { sharedQuery?: string }) {
  const utils = trpc.useUtils();
  const invoicesQuery = trpc.purchaseInvoices.list.useQuery();
  const vendorsQuery = trpc.vendors.listAll.useQuery();
  const contractsQuery = trpc.purchaseContracts.list.useQuery();
  const assetsQuery = trpc.assets.list.useQuery();
  const suppliesQuery = trpc.supplies.list.useQuery();
  const reconciliationQuery = trpc.purchaseInvoices.reconciliation.useQuery(undefined, { enabled: false });
  const [query, setQuery] = useState(sharedQuery);
  useEffect(() => { setQuery(sharedQuery); }, [sharedQuery]);
  const [vendorFilter, setVendorFilter] = useState("all");
  const [statusFilter, setStatusFilter] = useState<"all" | InvoiceStatus>("all");
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [formOpen, setFormOpen] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState<InvoiceForm>(emptyForm);
  const [lines, setLines] = useState<InvoiceLineForm[]>([]);
  const [queuedDocuments, setQueuedDocuments] = useState<QueuedDocument[]>([]);
  const [previewDocument, setPreviewDocument] = useState<QueuedDocument | null>(null);
  const [documentType, setDocumentType] = useState<QueuedDocument["documentType"]>("invoice_pdf");
  const createFileRef = useRef<HTMLInputElement>(null);
  const detailFileRef = useRef<HTMLInputElement>(null);
  const detailsQuery = trpc.purchaseInvoices.get.useQuery({ id: selectedId || 0 }, { enabled: selectedId !== null });
  const vendorsById = useMemo(() => new Map((vendorsQuery.data || []).map((vendor) => [vendor.id, vendor])), [vendorsQuery.data]);
  const contractsById = useMemo(() => new Map((contractsQuery.data || []).map((contract) => [contract.id, contract])), [contractsQuery.data]);
  const vendorOptions = useMemo(() => [{ value: "", label: "Chọn nhà cung cấp" }, ...(vendorsQuery.data || []).map((vendor) => ({ value: String(vendor.id), label: vendor.name, isActive: vendor.isActive }))], [vendorsQuery.data]);
  const vendorFilterOptions = useMemo(() => [{ value: "all", label: "Tất cả Nhà cung cấp" }, ...(vendorsQuery.data || []).map((vendor) => ({ value: String(vendor.id), label: vendor.name, isActive: vendor.isActive }))], [vendorsQuery.data]);
  const contractOptions = useMemo(() => [{ value: "", label: "Không gán Hợp đồng" }, ...(contractsQuery.data || []).filter((contract) => contract.status !== "cancelled").map((contract) => ({ value: String(contract.id), label: `${contract.referenceCode} · ${contract.title}` }))], [contractsQuery.data]);
  const statusOptions = [{ value: "all", label: "Tất cả trạng thái" }, ...Object.entries(invoiceStatuses).map(([value, meta]) => ({ value, label: meta.label }))];
  const invoices = invoicesQuery.data || [];
  const filteredInvoices = useMemo(() => invoices.filter((invoice) => {
    const vendor = vendorsById.get(invoice.vendorId)?.name || "";
    const matchesQuery = `${invoice.invoiceKey} ${vendor}`.toLocaleLowerCase("vi-VN").includes(query.trim().toLocaleLowerCase("vi-VN"));
    return matchesQuery && (vendorFilter === "all" || String(invoice.vendorId) === vendorFilter) && (statusFilter === "all" || invoice.status === statusFilter);
  }), [invoices, query, vendorFilter, statusFilter, vendorsById]);
  const hasActiveFilters = Boolean(query.trim()) || vendorFilter !== "all" || statusFilter !== "all";
  const invalidate = () => { void utils.purchaseInvoices.list.invalidate(); if (selectedId) void utils.purchaseInvoices.get.invalidate({ id: selectedId }); };
  const createInvoice = trpc.purchaseInvoices.create.useMutation({ onError: (error) => toast.error(error.message || "Không thể tạo Hóa đơn.") });
  const updateInvoice = trpc.purchaseInvoices.update.useMutation({ onSuccess: () => { toast.success("Đã cập nhật Hóa đơn mua bán."); setFormOpen(false); invalidate(); }, onError: (error) => toast.error(error.message || "Không thể cập nhật Hóa đơn.") });
  const createLine = trpc.purchaseInvoices.createLine.useMutation({ onError: (error) => toast.error(error.message || "Không thể lưu dòng Hóa đơn.") });
  const uploadDocument = trpc.purchaseInvoices.uploadDocument.useMutation({ onSuccess: () => { toast.success("Đã lưu chứng từ Hóa đơn."); invalidate(); }, onError: (error) => toast.error(error.message || "Không thể tải chứng từ Hóa đơn.") });
  const removeDocument = trpc.purchaseInvoices.removeDocument.useMutation({ onSuccess: () => { toast.success("Đã gỡ chứng từ Hóa đơn."); invalidate(); }, onError: (error) => toast.error(error.message || "Không thể gỡ chứng từ.") });
  const attachAsset = trpc.purchaseInvoices.attachAsset.useMutation({ onSuccess: () => { toast.success("Đã gán Tài sản vào dòng Hóa đơn."); invalidate(); void utils.assets.list.invalidate(); }, onError: (error) => toast.error(error.message || "Không thể gán Tài sản.") });
  const detachAsset = trpc.purchaseInvoices.detachAsset.useMutation({ onSuccess: () => { toast.success("Đã gỡ Tài sản khỏi Hóa đơn."); invalidate(); void utils.assets.list.invalidate(); }, onError: (error) => toast.error(error.message || "Không thể gỡ Tài sản.") });
  const receiveSupply = trpc.purchaseInvoices.receiveSupply.useMutation({ onSuccess: () => { toast.success("Đã tiếp nhận Phụ kiện và cập nhật tồn kho."); invalidate(); void utils.supplies.list.invalidate(); }, onError: (error) => toast.error(error.message || "Không thể tiếp nhận Phụ kiện.") });
  const createSupplyAndReceive = trpc.purchaseInvoices.createSupplyAndReceive.useMutation({ onSuccess: () => { toast.success("Đã tạo Phụ kiện và tiếp nhận vào kho theo Hóa đơn."); invalidate(); void utils.supplies.list.invalidate(); }, onError: (error) => toast.error(error.message || "Không thể tạo Phụ kiện từ Hóa đơn.") });

  const lineSubtotal = (line: InvoiceLineForm) => Number(line.quantity || 0) * Number(line.unitPrice || 0);
  const lineTax = (line: InvoiceLineForm) => lineSubtotal(line) * Number(line.taxRate || 0) / 100;
  const calculatedSubtotal = lines.reduce((total, line) => total + lineSubtotal(line), 0);
  const calculatedTax = lines.reduce((total, line) => total + lineTax(line), 0);
  const isSaving = createInvoice.isPending || updateInvoice.isPending || createLine.isPending || uploadDocument.isPending;

  const openCreate = () => { setEditingId(null); setForm(emptyForm); setLines([]); setQueuedDocuments([]); setFormOpen(true); };
  const openEdit = () => {
    const invoice = detailsQuery.data?.invoice;
    if (!invoice) return;
    setEditingId(invoice.id);
    setQueuedDocuments([]);
    setLines((detailsQuery.data?.lines || []).map((line) => ({ id: String(line.id), itemType: line.itemType, itemCode: line.itemCode || "", itemName: line.itemName, quantity: String(line.quantity), unit: line.unit || "", unitPrice: String(line.unitPrice).replace(/\D/g, ""), taxRate: String(line.taxRate).replace(/\D/g, ""), note: line.note || "" })));
    setForm({ invoiceNumber: invoice.invoiceNumber, invoiceSeries: invoice.invoiceSeries || "", invoiceTemplate: invoice.invoiceTemplate || "", invoiceType: invoice.invoiceType, status: invoice.status, vendorId: String(invoice.vendorId), purchaseContractId: invoice.purchaseContractId ? String(invoice.purchaseContractId) : "", issuedAt: toDateInput(invoice.issuedAt), receivedAt: toDateInput(invoice.receivedAt), subtotalAmount: String(invoice.subtotalAmount).replace(/\D/g, ""), taxAmount: String(invoice.taxAmount).replace(/\D/g, ""), totalAmount: String(invoice.totalAmount).replace(/\D/g, ""), note: invoice.note || "" });
    setFormOpen(true);
  };
  const selectContract = (value: string) => {
    const contract = contractsById.get(Number(value));
    const nextVendorId = contract?.vendorId ? String(contract.vendorId) : form.vendorId;
    setForm({ ...form, purchaseContractId: value, vendorId: nextVendorId });
  };
  const addDocuments = (files: FileList | File[]) => {
    const accepted: QueuedDocument[] = [];
    Array.from(files).forEach((file) => {
      const contentType = normalizedContentType(file);
      if (!acceptedInvoiceTypes.includes(contentType as typeof acceptedInvoiceTypes[number])) { toast.error(`${file.name}: chỉ hỗ trợ PDF, XML, PNG hoặc JPG.`); return; }
      if (file.size > 5 * 1024 * 1024) { toast.error(`${file.name}: tệp tối đa 5 MB.`); return; }
      const lowerName = file.name.toLowerCase();
      const inferredType: QueuedDocument["documentType"] = lowerName.endsWith(".xml") ? "invoice_xml" : lowerName.endsWith(".pdf") ? "invoice_pdf" : "scan";
      accepted.push({ id: crypto.randomUUID(), file, documentType: inferredType });
    });
    if (accepted.length) setQueuedDocuments((current) => [...current, ...accepted]);
  };
  const uploadInvoiceDocument = async (purchaseInvoiceId: number, item: QueuedDocument) => {
    const contentType = normalizedContentType(item.file) as "application/pdf" | "application/xml" | "image/png" | "image/jpeg";
    let dataUrl = await readFileAsDataUrl(item.file);
    if (contentType === "application/xml") dataUrl = dataUrl.replace(/^data:text\/xml;/, "data:application/xml;");
    await uploadDocument.mutateAsync({ purchaseInvoiceId, documentType: item.documentType, fileName: item.file.name, contentType, dataUrl });
  };
  const save = async () => {
    if (!form.invoiceNumber.trim() || !form.vendorId || !form.issuedAt || !form.totalAmount) { toast.error("Vui lòng nhập số Hóa đơn, Nhà cung cấp, ngày lập và tổng tiền."); return; }
    const payload = { invoiceNumber: form.invoiceNumber.trim(), invoiceSeries: form.invoiceSeries.trim() || null, invoiceTemplate: form.invoiceTemplate.trim() || null, invoiceType: form.invoiceType, status: form.status, vendorId: Number(form.vendorId), purchaseContractId: form.purchaseContractId ? Number(form.purchaseContractId) : null, issuedAt: toTimestamp(form.issuedAt)!, receivedAt: toTimestamp(form.receivedAt), currencyCode: "VND", exchangeRate: null, subtotalAmount: form.subtotalAmount || "0", taxAmount: form.taxAmount || "0", totalAmount: form.totalAmount, note: form.note.trim() || null };
    if (editingId) { updateInvoice.mutate({ id: editingId, ...payload }); return; }
    let created: { id: number };
    try { created = await createInvoice.mutateAsync(payload); } catch { return; }
    try {
      for (let index = 0; index < lines.length; index += 1) {
        const line = lines[index];
        if (!line.itemName.trim() || !line.unitPrice || !line.quantity) continue;
        const subtotal = lineSubtotal(line);
        const taxAmount = lineTax(line);
        await createLine.mutateAsync({ purchaseInvoiceId: created.id, lineNumber: index + 1, itemType: line.itemType, itemCode: line.itemCode.trim() || null, itemName: line.itemName.trim(), description: null, quantity: line.quantity, unit: line.unit.trim() || null, unitPrice: line.unitPrice, discountAmount: "0", taxRate: line.taxRate || "0", taxAmount: String(Math.round(taxAmount * 100) / 100), lineTotal: String(Math.round((subtotal + taxAmount) * 100) / 100), note: line.note.trim() || null });
      }
      for (const item of queuedDocuments) await uploadInvoiceDocument(created.id, item);
      toast.success(queuedDocuments.length ? "Đã tạo Hóa đơn và lưu chứng từ." : "Đã tạo Hóa đơn mua bán.");
    } catch {
      toast.error("Hóa đơn đã được tạo nhưng có dòng hoặc chứng từ chưa lưu. Vui lòng mở hồ sơ để hoàn tất.");
    }
    setFormOpen(false);
    setQueuedDocuments([]);
    setSelectedId(created.id);
    invalidate();
  };
  const detail = detailsQuery.data;
  const selectedInvoice = detail?.invoice;
  const exportReconciliation = async () => {
    const loading = toast.loading("Đang chuẩn bị Excel đối soát Hóa đơn–Tài sản...");
    try {
      const result = await reconciliationQuery.refetch();
      const rows = result.data || [];
      if (!rows.length) { toast.error("Chưa có dòng Hóa đơn để đối soát.", { id: loading }); return; }
      const sheetRows = rows.map((row) => ({
        "Số Hóa đơn": row.invoiceKey,
        "Nhà cung cấp": row.vendorName,
        "Ngày lập": dateLabel(row.invoiceIssuedAt),
        "Trạng thái Hóa đơn": invoiceStatuses[row.invoiceStatus]?.label || row.invoiceStatus,
        "Dòng": row.lineNumber,
        "Loại dòng": row.itemType === "asset" ? "Tài sản" : row.itemType === "supply" ? "Phụ kiện" : row.itemType === "service" ? "Dịch vụ" : "Khác",
        "Mã dòng": row.itemCode || "",
        "Tên dòng": row.itemName,
        "SL trên Hóa đơn": Number(row.invoicedQuantity),
        "Đơn vị": row.unit || "",
        "Đơn giá": Number(row.unitPrice),
        "Thành tiền": Number(row.lineTotal),
        "Tài sản đã gán": row.linkedAssets.map((asset) => `${asset.assetCode}${asset.serialNumber ? ` · ${asset.serialNumber}` : ""}`).join("; "),
        "Số Tài sản đã gán": row.linkedAssets.length,
        "Phụ kiện đã tiếp nhận": row.receivedQuantity,
        "Còn chờ tiếp nhận": row.itemType === "supply" ? Math.max(0, Number(row.invoicedQuantity) - row.receivedQuantity) : "",
      }));
      const workbook = XLSX.utils.book_new();
      const sheet = XLSX.utils.json_to_sheet(sheetRows);
      sheet["!cols"] = [{ wch: 24 }, { wch: 25 }, { wch: 14 }, { wch: 18 }, { wch: 8 }, { wch: 14 }, { wch: 15 }, { wch: 32 }, { wch: 16 }, { wch: 12 }, { wch: 16 }, { wch: 18 }, { wch: 42 }, { wch: 18 }, { wch: 20 }, { wch: 20 }];
      XLSX.utils.book_append_sheet(workbook, sheet, "Đối soát Hóa đơn");
      XLSX.writeFile(workbook, `doi-soat-hoa-don-tai-san-${new Date().toISOString().slice(0, 10)}.xlsx`);
      toast.success(`Đã xuất ${rows.length} dòng đối soát.`, { id: loading });
    } catch (error) { toast.error(error instanceof Error ? error.message : "Không thể xuất Excel đối soát.", { id: loading }); }
  };
  useEffect(() => {
    const heading = Array.from(document.querySelectorAll("h1")).find((item) => item.textContent?.trim() === "Hóa đơn mua bán");
    const header = heading?.parentElement?.parentElement;
    if (!header) return;
    let button = header.querySelector<HTMLButtonElement>("[data-invoice-reconciliation-export]");
    if (!button) {
      button = document.createElement("button");
      button.type = "button";
      button.dataset.invoiceReconciliationExport = "true";
      button.className = "inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-[#8BCDC6] bg-white px-3 text-xs font-extrabold text-[#087A6A] transition hover:bg-[#ECF8F7]";
      button.innerHTML = '<span aria-hidden="true">⇩</span> Xuất đối soát Excel';
      const actions = heading?.parentElement?.parentElement?.querySelector(".primary-action")?.parentElement;
      actions?.prepend(button);
    }
    const onExport = () => void exportReconciliation();
    button.addEventListener("click", onExport);
    return () => button?.removeEventListener("click", onExport);
  }, [reconciliationQuery.refetch]);
  useEffect(() => {
    if (!selectedInvoice || !detail) return;
    const drawerTitle = Array.from(document.querySelectorAll("h2")).find((item) => item.textContent?.trim() === selectedInvoice.invoiceKey);
    const lineSection = Array.from(drawerTitle?.parentElement?.parentElement?.parentElement?.querySelectorAll("section") || []).find((section) => section.textContent?.includes("Dòng Hóa đơn & Tài sản"));
    if (!lineSection) return;
    const existing = lineSection.querySelector<HTMLElement>("[data-invoice-line-operations]");
    existing?.remove();
    const host = document.createElement("div");
    host.dataset.invoiceLineOperations = "true";
    host.className = "mt-4";
    lineSection.append(host);
    const root = createRoot(host);
    root.render(<InvoiceLineOperationsPanel invoiceId={selectedInvoice.id} invoiceKey={selectedInvoice.invoiceKey} lines={detail.lines} linkedAssets={detail.linkedAssets} supplyReceipts={detail.supplyReceipts} assets={assetsQuery.data || []} supplies={suppliesQuery.data || []} onAttach={(assetId, lineId) => attachAsset.mutate({ purchaseInvoiceId: selectedInvoice.id, purchaseInvoiceLineId: lineId, assetId })} onDetach={(assetId) => detachAsset.mutate({ assetId })} onReceive={(input) => receiveSupply.mutate(input)} onCreateAndReceive={(input) => createSupplyAndReceive.mutate(input)} isWorking={attachAsset.isPending || detachAsset.isPending || receiveSupply.isPending || createSupplyAndReceive.isPending} />);
    return () => { root.unmount(); host.remove(); };
  }, [selectedInvoice?.id, selectedInvoice?.invoiceKey, detail?.lines, detail?.linkedAssets, detail?.supplyReceipts, assetsQuery.data, suppliesQuery.data, attachAsset.isPending, detachAsset.isPending, receiveSupply.isPending, createSupplyAndReceive.isPending]);

  return <div className="min-h-screen bg-[#F4F7FB] px-4 py-7 sm:px-6 lg:px-9 lg:py-8"><div className="mx-auto max-w-[1500px]">
    <div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[.16em] text-[#0F8C8C]"><ReceiptText size={14} />Mua sắm & chứng từ</div><h1 className="mt-1 font-display text-3xl font-extrabold text-[#102A43]">Hóa đơn mua bán</h1><p className="mt-1 max-w-2xl text-sm text-[#71869A]">Theo dõi hóa đơn là nguồn mua trực tiếp của Tài sản và Phụ kiện; Hợp đồng chỉ là liên kết tùy chọn.</p></div><button type="button" onClick={openCreate} className="primary-action"><Plus size={16} />Tạo hóa đơn</button></div>
    <section className="mt-6 grid gap-3 sm:grid-cols-3"><Metric label="Tổng hóa đơn" value={invoices.length} icon={<ReceiptText size={18} />} /><Metric label="Đã phát hành" value={invoices.filter((item) => item.status === "issued").length} icon={<FileText size={18} />} tone="teal" /><Metric label="Nháp cần hoàn thiện" value={invoices.filter((item) => item.status === "draft").length} icon={<Pencil size={18} />} tone="blue" /></section>
    <section className="mt-5 rounded-2xl border border-[#DFE9F0] bg-white p-4 shadow-[0_8px_24px_rgba(16,42,67,0.05)]"><div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-[minmax(0,1fr)_200px_190px_auto] xl:items-center"><div className="relative min-w-0 sm:col-span-2 xl:col-span-1"><Search className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-[#8AA0B6]" size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} className="field-input !pl-11" placeholder="Tìm số, ký hiệu hóa đơn hoặc nhà cung cấp..." /></div><SearchableSelect value={vendorFilter} onChange={setVendorFilter} options={vendorFilterOptions} placeholder="Tất cả Nhà cung cấp" searchPlaceholder="Tìm Nhà cung cấp..." /><SearchableSelect value={statusFilter} onChange={(value) => setStatusFilter(value as "all" | InvoiceStatus)} options={statusOptions} placeholder="Tất cả trạng thái" searchPlaceholder="Tìm trạng thái..." /><button type="button" onClick={() => { setQuery(""); setVendorFilter("all"); setStatusFilter("all"); }} disabled={!hasActiveFilters} className="inline-flex h-10 shrink-0 items-center justify-center gap-2 whitespace-nowrap rounded-lg border border-[#DDE7F0] bg-white px-3 text-xs font-bold text-[#60758A] transition hover:bg-[#F7FAFC] disabled:cursor-not-allowed disabled:opacity-50"><X size={14} />Xóa bộ lọc</button></div></section>
    <section className="mt-5 overflow-hidden rounded-2xl border border-[#DFE9F0] bg-white shadow-[0_8px_24px_rgba(16,42,67,0.05)]"><div className="overflow-x-auto"><table className={`w-full text-left text-xs ${filteredInvoices.length || invoicesQuery.isLoading ? "min-w-[940px]" : "min-w-0"}`}><thead className="bg-[#F8FBFC] text-[10px] uppercase tracking-[.1em] text-[#8AA0B6]"><tr><th className="px-5 py-4">Hóa đơn</th><th className="px-4 py-4">Nhà cung cấp</th><th className="px-4 py-4">Hợp đồng</th><th className="px-4 py-4">Ngày lập</th><th className="px-4 py-4 text-right">Tổng tiền</th><th className="px-4 py-4">Trạng thái</th><th className="px-5 py-4 text-right">Thao tác</th></tr></thead><tbody>{invoicesQuery.isLoading ? <tr><td colSpan={7} className="p-10 text-center text-sm text-[#71869A]">Đang tải danh sách Hóa đơn...</td></tr> : filteredInvoices.map((invoice) => { const status = invoiceStatuses[invoice.status]; const contract = invoice.purchaseContractId ? contractsById.get(invoice.purchaseContractId) : null; return <tr key={invoice.id} className="border-t border-[#EDF2F5] transition hover:bg-[#FBFCFD]"><td className="px-5 py-4"><button type="button" onClick={() => setSelectedId(invoice.id)} className="text-left"><div className="font-mono text-[11px] font-extrabold text-[#087A6A] hover:underline">{invoice.invoiceKey}</div><div className="mt-1 text-[10px] text-[#71869A]">{invoiceTypes[invoice.invoiceType]}</div></button></td><td className="px-4 py-4 font-medium text-[#60758A]">{vendorsById.get(invoice.vendorId)?.name || "Nhà cung cấp đã xóa"}</td><td className="px-4 py-4 text-[#60758A]">{contract ? <span className="font-mono text-[10px]">{contract.referenceCode}</span> : "—"}</td><td className="px-4 py-4 text-[#60758A]">{dateLabel(invoice.issuedAt)}</td><td className="px-4 py-4 text-right font-bold text-[#193B57]">{money(invoice.totalAmount)}</td><td className="px-4 py-4"><span className={`rounded-full px-2 py-1 text-[10px] font-extrabold ${status.tone}`}>{status.label}</span></td><td className="px-5 py-4 text-right"><button type="button" onClick={() => setSelectedId(invoice.id)} className="text-[11px] font-extrabold text-[#2666A8] hover:underline">Xem hồ sơ</button></td></tr>; })}{!invoicesQuery.isLoading && !filteredInvoices.length && <tr><td colSpan={7} className="p-12 text-center"><div className="text-sm font-bold text-[#193B57]">Chưa có Hóa đơn phù hợp</div><p className="mt-1 text-xs text-[#71869A]">Tạo hóa đơn đầu tiên để dùng làm nguồn mua trực tiếp cho Tài sản và Phụ kiện.</p></td></tr>}</tbody></table></div></section>
  </div>
  {selectedId !== null && <><button type="button" aria-label="Đóng hồ sơ Hóa đơn" onClick={() => setSelectedId(null)} className="fixed inset-0 z-[85] bg-[#102A43]/25 backdrop-blur-[1px]" /><aside className="fixed inset-y-0 right-0 z-[86] w-full max-w-2xl overflow-y-auto border-l border-[#DFE9F0] bg-white p-5 shadow-2xl sm:p-6"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><div className="text-[10px] font-extrabold uppercase tracking-[.14em] text-[#0F8C8C]">Hồ sơ hóa đơn</div><h2 className="mt-1 truncate font-display text-xl font-extrabold text-[#102A43]">{selectedInvoice?.invoiceKey || "Đang tải..."}</h2><p className="mt-1 text-xs text-[#71869A]">{selectedInvoice ? `${vendorsById.get(selectedInvoice.vendorId)?.name || "Nhà cung cấp"} · ${invoiceTypes[selectedInvoice.invoiceType]}` : "Đang lấy thông tin Hóa đơn"}</p></div><button type="button" onClick={() => setSelectedId(null)} className="drawer-close-action" aria-label="Đóng"><X size={18} /></button></div>{detailsQuery.isLoading ? <div className="grid min-h-[320px] place-items-center text-sm font-semibold text-[#71869A]"><Loader2 className="mr-2 animate-spin" size={18} />Đang tải hồ sơ Hóa đơn...</div> : !detail || !selectedInvoice ? <div className="mt-6 rounded-xl border border-[#F2B18B] bg-[#FFF2E9] p-4 text-sm text-[#9E3F12]">Không thể tải chi tiết Hóa đơn.</div> : <div className="mt-6 space-y-5"><section className="rounded-xl border border-[#DDE7F0] bg-[#FBFCFD] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="font-mono text-sm font-extrabold text-[#193B57]">{selectedInvoice.invoiceKey}</div><div className="mt-1 text-xs text-[#71869A]">{vendorsById.get(selectedInvoice.vendorId)?.name || "Nhà cung cấp đã xóa"}</div></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-extrabold ${invoiceStatuses[selectedInvoice.status].tone}`}>{invoiceStatuses[selectedInvoice.status].label}</span></div><dl className="mt-4 grid gap-3 text-xs sm:grid-cols-2"><Info label="Ngày lập" value={dateLabel(selectedInvoice.issuedAt)} /><Info label="Tổng tiền" value={money(selectedInvoice.totalAmount)} /><Info label="Hợp đồng" value={selectedInvoice.purchaseContractId ? contractsById.get(selectedInvoice.purchaseContractId)?.referenceCode || "Hợp đồng không còn khả dụng" : "Không gán Hợp đồng"} /><Info label="Ngày nhận" value={dateLabel(selectedInvoice.receivedAt)} /></dl><div className="mt-4"><button type="button" onClick={openEdit} className="filter-action"><Pencil size={14} />Chỉnh sửa</button></div></section><section className="rounded-xl border border-[#DFE9F0] p-4"><div className="flex items-center justify-between"><div><div className="flex items-center gap-2 text-sm font-extrabold text-[#193B57]"><Link2 size={16} className="text-[#087A6A]" />Dòng Hóa đơn & Tài sản</div><p className="mt-1 text-[11px] text-[#71869A]">Tài sản được liên kết trực tiếp theo Hóa đơn và có thể chỉ rõ dòng tương ứng.</p></div><span className="rounded-full bg-[#E6F6F2] px-2.5 py-1 text-[10px] font-extrabold text-[#087A6A]">{detail.lines.length} dòng · {detail.linkedAssets.length} tài sản</span></div><div className="mt-3 space-y-2">{detail.lines.map((line) => <div key={line.id} className="rounded-lg border border-[#E7EEF3] bg-[#FBFCFD] px-3 py-2.5"><div className="flex justify-between gap-3"><div className="min-w-0"><span className="font-mono text-[10px] font-bold text-[#087A6A]">#{line.lineNumber}</span><span className="ml-2 text-xs font-bold text-[#193B57]">{line.itemName}</span></div><span className="shrink-0 text-xs font-extrabold text-[#193B57]">{money(line.lineTotal)}</span></div><div className="mt-1 text-[10px] text-[#71869A]">{line.itemType === "asset" ? "Tài sản" : line.itemType === "supply" ? "Phụ kiện" : "Dịch vụ/khác"} · {line.quantity} {line.unit || ""}</div></div>)}{!detail.lines.length && <p className="rounded-lg border border-dashed border-[#D8E3EA] px-3 py-4 text-center text-xs text-[#8AA0B6]">Chưa có dòng Hóa đơn.</p>}</div><div className="mt-3 rounded-lg bg-[#F4FBFA] px-3 py-2.5 text-[11px] text-[#4B8884]">Tài sản liên kết: {detail.linkedAssets.length ? detail.linkedAssets.map((asset) => `${asset.assetCode} · ${asset.name}`).join("; ") : "Chưa có tài sản nào."}</div></section><section className="rounded-xl border border-[#CDE5E5] bg-[#F4FBFA] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-sm font-extrabold text-[#193B57]"><Paperclip size={16} className="text-[#087A6A]" />Chứng từ Hóa đơn</div><p className="mt-1 text-[11px] text-[#4B8884]">PDF, XML và ảnh scan được lưu tập trung theo Hóa đơn.</p></div><div className="w-[160px]"><SearchableSelect value={documentType} onChange={(value) => setDocumentType(value as QueuedDocument["documentType"])} options={documentTypes} placeholder="Loại chứng từ" searchPlaceholder="Tìm loại chứng từ..." /></div></div><input ref={detailFileRef} type="file" accept=".pdf,.xml,.png,.jpg,.jpeg" className="hidden" onChange={(event) => { const file = event.target.files?.[0]; if (file) { const item: QueuedDocument = { id: crypto.randomUUID(), file, documentType }; if (acceptedInvoiceTypes.includes(normalizedContentType(file) as typeof acceptedInvoiceTypes[number]) && file.size <= 5 * 1024 * 1024) void uploadInvoiceDocument(selectedInvoice.id, item); else toast.error("Chỉ hỗ trợ PDF, XML, PNG, JPG; tối đa 5 MB."); } event.currentTarget.value = ""; }} /><button type="button" onClick={() => detailFileRef.current?.click()} disabled={uploadDocument.isPending} className="mt-3 inline-flex h-9 items-center gap-2 rounded-lg bg-[#087A6A] px-3 text-[11px] font-extrabold text-white transition hover:bg-[#066254] disabled:opacity-60"><Paperclip size={14} />{uploadDocument.isPending ? "Đang lưu tệp..." : "Tải chứng từ"}</button><div className="mt-3 space-y-2">{detail.documents.map((document) => <div key={document.id} className="flex items-center gap-3 rounded-lg border border-[#CDE5E5] bg-white px-3 py-2.5"><DocumentIcon contentType={document.contentType} /><a href={document.url} target="_blank" rel="noreferrer" className="min-w-0 flex-1 truncate text-xs font-bold text-[#193B57] hover:text-[#087A6A] hover:underline">{document.fileName}</a><button type="button" aria-label={`Gỡ ${document.fileName}`} onClick={() => removeDocument.mutate({ id: document.id })} className="grid h-7 w-7 place-items-center rounded-md text-[#8AA0B6] transition hover:bg-[#FDEDEE] hover:text-[#B44545]"><X size={14} /></button></div>)}{!detail.documents.length && <p className="rounded-lg border border-dashed border-[#8BCDC6] bg-white/60 px-3 py-4 text-center text-xs text-[#71869A]">Chưa có chứng từ. Tải bản PDF, XML hoặc ảnh scan Hóa đơn.</p>}</div></section></div>}</aside></>}
  {formOpen && <><button type="button" aria-label="Đóng form Hóa đơn" onClick={() => !isSaving && setFormOpen(false)} className="fixed inset-0 z-[100] bg-[#102A43]/40 backdrop-blur-sm" /><section role="dialog" aria-modal="true" className="fixed left-1/2 top-1/2 z-[101] max-h-[92vh] w-[calc(100%-2rem)] max-w-4xl -translate-x-1/2 -translate-y-1/2 overflow-y-auto rounded-2xl border border-[#DFE9F0] bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-[#E7EEF3] px-5 py-4 sm:px-6"><div><div className="text-[10px] font-extrabold uppercase tracking-[.14em] text-[#0F8C8C]">Nguồn mua trực tiếp</div><h2 className="mt-1 font-display text-xl font-extrabold text-[#102A43]">{editingId ? "Cập nhật Hóa đơn" : "Tạo Hóa đơn mua bán"}</h2><p className="mt-1 text-xs text-[#71869A]">Hợp đồng là tùy chọn. Một Hóa đơn có thể chứa nhiều Tài sản và Phụ kiện.</p></div><button type="button" onClick={() => !isSaving && setFormOpen(false)} className="drawer-close-action" aria-label="Đóng"><X size={18} /></button></div><div className="grid gap-4 p-5 sm:grid-cols-2 sm:p-6"><Field label="Số Hóa đơn *"><input autoFocus value={form.invoiceNumber} onChange={(event) => setForm({ ...form, invoiceNumber: event.target.value })} placeholder="VD: 00001234" className="field-input font-mono" /></Field><Field label="Ký hiệu Hóa đơn"><input value={form.invoiceSeries} onChange={(event) => setForm({ ...form, invoiceSeries: event.target.value.toUpperCase() })} placeholder="VD: 1C26TTH" className="field-input font-mono" /></Field><Field label="Mẫu số"><input value={form.invoiceTemplate} onChange={(event) => setForm({ ...form, invoiceTemplate: event.target.value.toUpperCase() })} placeholder="VD: 01GTKT0/001" className="field-input font-mono" /></Field><SelectField label="Loại Hóa đơn"><SearchableSelect value={form.invoiceType} onChange={(value) => setForm({ ...form, invoiceType: value as InvoiceType })} options={Object.entries(invoiceTypes).map(([value, label]) => ({ value, label }))} placeholder="Chọn loại Hóa đơn" searchPlaceholder="Tìm loại Hóa đơn..." /></SelectField><SelectField label="Nhà cung cấp *"><SearchableSelect value={form.vendorId} onChange={(value) => setForm({ ...form, vendorId: value })} options={vendorOptions} placeholder="Chọn nhà cung cấp" searchPlaceholder="Tìm nhà cung cấp..." /></SelectField><SelectField label="Hợp đồng (tùy chọn)"><SearchableSelect value={form.purchaseContractId} onChange={selectContract} options={contractOptions} placeholder="Không gán Hợp đồng" searchPlaceholder="Tìm Hợp đồng..." /></SelectField><Field label="Ngày lập Hóa đơn *"><DatePickerField value={form.issuedAt} onChange={(value) => setForm({ ...form, issuedAt: value })} placeholder="Chọn ngày lập" aria-label="Ngày lập Hóa đơn" /></Field><Field label="Ngày nhận"><DatePickerField value={form.receivedAt} onChange={(value) => setForm({ ...form, receivedAt: value })} placeholder="Chọn ngày nhận" aria-label="Ngày nhận Hóa đơn" /></Field><Field label="Tiền trước thuế"><CurrencyInput value={form.subtotalAmount} onChange={(value) => setForm({ ...form, subtotalAmount: value })} placeholder={calculatedSubtotal ? String(calculatedSubtotal) : "0"} showWords aria-label="Tiền trước thuế" /></Field><Field label="Thuế GTGT"><CurrencyInput value={form.taxAmount} onChange={(value) => setForm({ ...form, taxAmount: value })} placeholder={calculatedTax ? String(calculatedTax) : "0"} showWords aria-label="Thuế GTGT" /></Field><Field label="Tổng thanh toán *"><CurrencyInput value={form.totalAmount} onChange={(value) => setForm({ ...form, totalAmount: value })} placeholder={calculatedSubtotal + calculatedTax ? String(calculatedSubtotal + calculatedTax) : "Ví dụ: 250.000.000"} showWords aria-label="Tổng thanh toán Hóa đơn" /></Field><SelectField label="Trạng thái"><SearchableSelect value={form.status} onChange={(value) => setForm({ ...form, status: value as InvoiceStatus })} options={Object.entries(invoiceStatuses).map(([value, meta]) => ({ value, label: meta.label }))} placeholder="Chọn trạng thái" searchPlaceholder="Tìm trạng thái..." /></SelectField><div className="sm:col-span-2"><section className="rounded-xl border border-[#DFE9F0] bg-[#FBFCFD] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-sm font-extrabold text-[#193B57]"><ReceiptText size={16} className="text-[#087A6A]" />Dòng Hóa đơn <span className="font-medium text-[#8AA0B6]">(tùy chọn)</span></div><p className="mt-1 text-[11px] text-[#71869A]">Khai báo các dòng tài sản, phụ kiện hoặc dịch vụ để đối chiếu nguồn mua.</p></div><button type="button" onClick={() => setLines((current) => [...current, newLine()])} className="filter-action"><Plus size={14} />Thêm dòng</button></div><div className="mt-3 space-y-3">{lines.map((line, index) => <InvoiceLineEditor key={line.id} line={line} index={index} onChange={(changes) => setLines((current) => current.map((item) => item.id === line.id ? { ...item, ...changes } : item))} onRemove={() => setLines((current) => current.filter((item) => item.id !== line.id))} />)}{!lines.length && <p className="rounded-lg border border-dashed border-[#D8E3EA] px-3 py-4 text-center text-xs text-[#8AA0B6]">Chưa có dòng Hóa đơn. Có thể thêm sau trong hồ sơ Hóa đơn.</p>}</div></section></div>{!editingId && <div className="sm:col-span-2"><section className="rounded-xl border border-dashed border-[#9ADBD3] bg-[#F7FCFC] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-sm font-extrabold text-[#193B57]"><Paperclip size={16} className="text-[#087A6A]" />Chứng từ Hóa đơn <span className="font-medium text-[#8AA0B6]">(tùy chọn)</span></div><p className="mt-1 text-[11px] leading-5 text-[#4B8884]">Chọn nhiều tệp PDF, XML hoặc ảnh scan. Mỗi tệp tối đa 5 MB; hệ thống lưu cùng Hóa đơn sau khi tạo.</p></div><button type="button" onClick={() => createFileRef.current?.click()} className="inline-flex h-9 items-center gap-2 rounded-lg border border-[#CDE5E5] bg-white px-3 text-[11px] font-extrabold text-[#087A6A] transition hover:bg-[#ECF8F7]"><Paperclip size={14} />Chọn tệp</button></div><input ref={createFileRef} type="file" multiple accept=".pdf,.xml,.png,.jpg,.jpeg" className="hidden" onChange={(event) => { if (event.target.files) addDocuments(event.target.files); event.currentTarget.value = ""; }} /><div className="mt-3 grid gap-2 md:grid-cols-2">{queuedDocuments.map((item) => <QueuedDocumentCard key={item.id} item={item} onTypeChange={(documentType) => setQueuedDocuments((current) => current.map((entry) => entry.id === item.id ? { ...entry, documentType } : entry))} onPreview={() => setPreviewDocument(item)} onRemove={() => setQueuedDocuments((current) => current.filter((entry) => entry.id !== item.id))} />)}{!queuedDocuments.length && <p className="rounded-lg border border-dashed border-[#8BCDC6] bg-white/60 px-3 py-4 text-center text-xs text-[#71869A]">Chưa chọn chứng từ. Bạn vẫn có thể tải thêm sau khi tạo Hóa đơn.</p>}</div></section></div>}<div className="sm:col-span-2"><Field label="Ghi chú"><textarea value={form.note} onChange={(event) => setForm({ ...form, note: event.target.value })} className="field-input min-h-24" placeholder="Thông tin thanh toán, tiếp nhận hoặc lưu ý đối chiếu..." /></Field></div></div><div className="flex justify-end gap-2 border-t border-[#E7EEF3] px-5 py-4 sm:px-6"><button type="button" disabled={isSaving} onClick={() => setFormOpen(false)} className="filter-action">Hủy</button><button type="button" disabled={isSaving} onClick={() => void save()} className="primary-action">{isSaving && <Loader2 size={15} className="animate-spin" />}{isSaving ? "Đang lưu..." : editingId ? "Lưu thay đổi" : "Tạo Hóa đơn"}</button></div></section></>}
  {previewDocument && <DocumentPreview item={previewDocument} onClose={() => setPreviewDocument(null)} />}
  </div>;
}

function InvoiceLineEditor({ line, index, onChange, onRemove }: { line: InvoiceLineForm; index: number; onChange: (changes: Partial<InvoiceLineForm>) => void; onRemove: () => void }) { const subtotal = Number(line.quantity || 0) * Number(line.unitPrice || 0); const total = subtotal * (1 + Number(line.taxRate || 0) / 100); return <div className="rounded-lg border border-[#E1EBF1] bg-white p-3"><div className="mb-2 flex items-center justify-between"><span className="text-[10px] font-extrabold uppercase tracking-[.1em] text-[#4B8884]">Dòng {index + 1}</span><button type="button" onClick={onRemove} className="text-[10px] font-bold text-[#B44545] hover:underline">Xóa dòng</button></div><div className="grid gap-2 sm:grid-cols-6"><SearchableSelect value={line.itemType} onChange={(value) => onChange({ itemType: value as InvoiceLineForm["itemType"] })} options={[{ value: "asset", label: "Tài sản" }, { value: "supply", label: "Phụ kiện" }, { value: "service", label: "Dịch vụ" }, { value: "other", label: "Khác" }]} placeholder="Loại" searchPlaceholder="Tìm loại..." /><input value={line.itemName} onChange={(event) => onChange({ itemName: event.target.value })} className="field-input sm:col-span-2" placeholder="Tên hàng hóa/dịch vụ" /><input value={line.quantity} inputMode="decimal" onChange={(event) => onChange({ quantity: event.target.value.replace(/[^0-9.]/g, "") })} className="field-input" placeholder="SL" /><input value={line.unit} onChange={(event) => onChange({ unit: event.target.value })} className="field-input" placeholder="ĐVT" /><CurrencyInput value={line.unitPrice} onChange={(value) => onChange({ unitPrice: value })} placeholder="Đơn giá" aria-label={`Đơn giá dòng ${index + 1}`} /></div><div className="mt-2 flex items-center justify-between gap-2 text-[10px] text-[#71869A]"><input value={line.taxRate} inputMode="decimal" onChange={(event) => onChange({ taxRate: event.target.value.replace(/[^0-9.]/g, "") })} className="h-7 w-20 rounded border border-[#DDE7F0] px-2 text-xs" placeholder="VAT %" /><span className="font-bold text-[#193B57]">Tạm tính: {money(total)}</span></div></div>; }
type InvoiceOperationLine = { id: number; lineNumber: number; itemType: "asset" | "supply" | "service" | "other"; itemName: string; quantity: string; unit: string | null; unitPrice: string; taxRate: string; taxAmount: string };
type InvoiceOperationAsset = { id: number; assetCode: string; name: string; serialNumber: string | null; purchaseInvoiceId: number | null; purchaseInvoiceLineId: number | null; status: string };
type InvoiceOperationSupply = { id: number; code: string; name: string; unit: string; stockQuantity: string; isActive: boolean };
type InvoiceOperationReceipt = { id: number; purchaseInvoiceLineId: number; supplyId: number; receivedQuantity: string; status: "received" | "void"; supply: InvoiceOperationSupply | null };
type ReceiveSupplyInput = { purchaseInvoiceLineId: number; supplyId: number; receivedQuantity: string; unitCost: string | null; taxRate: string; taxAmount: string; totalAmount: string; receivedAt: number; note: string | null };
function LegacyInvoiceLineOperationsPanel({ invoiceId, invoiceKey, lines, linkedAssets, supplyReceipts, assets, supplies, onAttach, onDetach, onReceive, isWorking }: { invoiceId: number; invoiceKey: string; lines: InvoiceOperationLine[]; linkedAssets: InvoiceOperationAsset[]; supplyReceipts: InvoiceOperationReceipt[]; assets: InvoiceOperationAsset[]; supplies: InvoiceOperationSupply[]; onAttach: (assetId: number, lineId: number | null) => void; onDetach: (assetId: number) => void; onReceive: (input: ReceiveSupplyInput) => void; isWorking: boolean }) {
  const [assetDrafts, setAssetDrafts] = useState<Record<number, string>>({});
  const [supplyDrafts, setSupplyDrafts] = useState<Record<number, { supplyId: string; quantity: string }>>({});
  const activeAssets = assets.filter((asset) => asset.status !== "retired" && asset.status !== "returned_to_vendor");
  return <section className="mt-4 rounded-xl border border-[#CDE5E5] bg-[#F4FBFA] p-3"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-sm font-extrabold text-[#193B57]"><Link2 size={15} className="text-[#087A6A]" />Phân bổ nguồn mua theo dòng</div><p className="mt-1 text-[11px] text-[#4B8884]">Gán trực tiếp Tài sản vào dòng loại Tài sản, hoặc tiếp nhận Phụ kiện vào kho theo dòng loại Phụ kiện.</p></div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-bold text-[#087A6A]">{invoiceKey}</span></div><div className="mt-3 space-y-3">{lines.filter((line) => line.itemType === "asset" || line.itemType === "supply").map((line) => {
    const lineAssets = linkedAssets.filter((asset) => asset.purchaseInvoiceLineId === line.id);
    const receipts = supplyReceipts.filter((receipt) => receipt.purchaseInvoiceLineId === line.id && receipt.status === "received");
    const received = receipts.reduce((total, receipt) => total + Number(receipt.receivedQuantity), 0);
    const draft = supplyDrafts[line.id] || { supplyId: "", quantity: "" };
    const remaining = Math.max(0, Number(line.quantity) - received);
    return <div key={line.id} className="rounded-lg border border-[#CDE5E5] bg-white p-3"><div className="flex flex-wrap items-start justify-between gap-2"><div><div className="text-xs font-extrabold text-[#193B57]">#{line.lineNumber} · {line.itemName}</div><div className="mt-1 text-[10px] text-[#71869A]">{line.itemType === "asset" ? "Tài sản" : "Phụ kiện"} · SL Hóa đơn: {line.quantity} {line.unit || ""} · {money(line.unitPrice)}/đơn vị</div></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${line.itemType === "asset" ? "bg-[#EAF3FF] text-[#2666A8]" : "bg-[#FFF5DC] text-[#A86B00]"}`}>{line.itemType === "asset" ? `${lineAssets.length} Tài sản đã gán` : `Đã nhập ${received}/${line.quantity}`}</span></div>{line.itemType === "asset" ? <div className="mt-3"><div className="flex flex-col gap-2 sm:flex-row"><select value={assetDrafts[line.id] || ""} onChange={(event) => setAssetDrafts((current) => ({ ...current, [line.id]: event.target.value }))} className="field-input min-w-0 flex-1" aria-label={`Chọn Tài sản cho dòng ${line.lineNumber}`}><option value="">Chọn Tài sản cần gán</option>{activeAssets.filter((asset) => !asset.purchaseInvoiceId || asset.purchaseInvoiceId === invoiceId).map((asset) => <option key={asset.id} value={asset.id}>{asset.assetCode} · {asset.name}{asset.serialNumber ? ` · ${asset.serialNumber}` : ""}</option>)}</select><button type="button" disabled={!assetDrafts[line.id] || isWorking} onClick={() => onAttach(Number(assetDrafts[line.id]), line.id)} className="filter-action whitespace-nowrap"><Link2 size={14} />Gán Tài sản</button></div><div className="mt-2 flex flex-wrap gap-1.5">{lineAssets.map((asset) => <span key={asset.id} className="inline-flex items-center gap-1 rounded-md border border-[#C9DDF5] bg-[#F2F8FF] px-2 py-1 text-[10px] font-bold text-[#2666A8]">{asset.assetCode}<button type="button" aria-label={`Gỡ ${asset.assetCode}`} disabled={isWorking} onClick={() => onDetach(asset.id)} className="ml-0.5 text-[#6F94C0] hover:text-[#B44545]"><X size={12} /></button></span>)}{!lineAssets.length && <span className="text-[10px] text-[#8AA0B6]">Chưa gán Tài sản nào.</span>}</div></div> : <div className="mt-3"><div className="grid gap-2 sm:grid-cols-[minmax(0,1fr)_110px_auto]"><select value={draft.supplyId} onChange={(event) => setSupplyDrafts((current) => ({ ...current, [line.id]: { ...draft, supplyId: event.target.value } }))} className="field-input" aria-label={`Chọn Phụ kiện cho dòng ${line.lineNumber}`}><option value="">Chọn Phụ kiện có sẵn để nhập kho</option>{supplies.filter((supply) => supply.isActive).map((supply) => <option key={supply.id} value={supply.id}>{supply.code} · {supply.name} · tồn {Number(supply.stockQuantity).toLocaleString("vi-VN")} {supply.unit}</option>)}</select><input value={draft.quantity} inputMode="decimal" onChange={(event) => setSupplyDrafts((current) => ({ ...current, [line.id]: { ...draft, quantity: event.target.value.replace(/[^0-9.]/g, "") } }))} className="field-input" placeholder={`Tối đa ${remaining}`} /><button type="button" disabled={!draft.supplyId || !draft.quantity || Number(draft.quantity) <= 0 || Number(draft.quantity) > remaining || isWorking} onClick={() => onReceive({ purchaseInvoiceLineId: line.id, supplyId: Number(draft.supplyId), receivedQuantity: draft.quantity, unitCost: line.unitPrice || null, taxRate: line.taxRate || "0", taxAmount: String(Math.round(Number(draft.quantity) * Number(line.unitPrice || 0) * Number(line.taxRate || 0) / 100 * 100) / 100), totalAmount: String(Math.round(Number(draft.quantity) * Number(line.unitPrice || 0) * (1 + Number(line.taxRate || 0) / 100) * 100) / 100), receivedAt: Date.now(), note: `Tiếp nhận từ Hóa đơn ${invoiceKey} · dòng ${line.lineNumber}` })} className="filter-action whitespace-nowrap"><Plus size={14} />Tiếp nhận</button></div><div className="mt-2 text-[10px] text-[#71869A]">Còn có thể tiếp nhận: <b className="text-[#193B57]">{remaining} {line.unit || ""}</b>. Mỗi lần tiếp nhận sẽ cộng tồn kho và tạo biến động kho.</div><div className="mt-2 flex flex-wrap gap-1.5">{receipts.map((receipt) => <span key={receipt.id} className="rounded-md border border-[#D7E9D6] bg-[#F3FBF2] px-2 py-1 text-[10px] font-bold text-[#4A8A46]">+{receipt.receivedQuantity} {receipt.supply?.unit || line.unit || ""} · {receipt.supply?.code || "Phụ kiện"}</span>)}{!receipts.length && <span className="text-[10px] text-[#8AA0B6]">Chưa có lần tiếp nhận.</span>}</div></div>}</div>;
  })}</div>{!lines.some((line) => line.itemType === "asset" || line.itemType === "supply") && <p className="mt-3 rounded-lg border border-dashed border-[#B7D9D5] bg-white/70 px-3 py-4 text-center text-xs text-[#71869A]">Thêm dòng loại Tài sản hoặc Phụ kiện để thực hiện phân bổ nguồn mua.</p>}</section>;
}
function QueuedDocumentCard({ item, onTypeChange, onPreview, onRemove }: { item: QueuedDocument; onTypeChange: (type: QueuedDocument["documentType"]) => void; onPreview: () => void; onRemove: () => void }) { return <div className="rounded-lg border border-[#CDE5E5] bg-white p-3"><div className="flex items-start gap-2"><DocumentIcon contentType={normalizedContentType(item.file)} /><div className="min-w-0 flex-1"><div className="truncate text-xs font-bold text-[#193B57]">{item.file.name}</div><div className="mt-0.5 text-[10px] text-[#71869A]">{(item.file.size / 1024).toLocaleString("vi-VN", { maximumFractionDigits: 1 })} KB</div></div><button type="button" onClick={onRemove} aria-label={`Bỏ ${item.file.name}`} className="text-[#8AA0B6] hover:text-[#B44545]"><X size={15} /></button></div><div className="mt-2 flex gap-2"><div className="min-w-0 flex-1"><SearchableSelect value={item.documentType} onChange={(value) => onTypeChange(value as QueuedDocument["documentType"])} options={documentTypes} placeholder="Loại tệp" searchPlaceholder="Tìm loại..." /></div><button type="button" onClick={onPreview} className="inline-flex h-9 items-center gap-1 rounded-lg border border-[#DDE7F0] px-2.5 text-[10px] font-bold text-[#2666A8] hover:bg-[#F4F8FC]"><Eye size={13} />Xem</button></div></div>; }
function DocumentPreview({ item, onClose }: { item: QueuedDocument; onClose: () => void }) { const [xml, setXml] = useState(""); const contentType = normalizedContentType(item.file); const objectUrl = useMemo(() => URL.createObjectURL(item.file), [item.file]); useEffect(() => { if (contentType === "application/xml") void item.file.text().then(setXml); return () => URL.revokeObjectURL(objectUrl); }, [contentType, item.file, objectUrl]); return <><button type="button" aria-label="Đóng xem trước chứng từ" onClick={onClose} className="fixed inset-0 z-[120] bg-[#102A43]/55 backdrop-blur-sm" /><section role="dialog" aria-modal="true" className="fixed left-1/2 top-1/2 z-[121] flex max-h-[90vh] w-[calc(100%-2rem)] max-w-4xl -translate-x-1/2 -translate-y-1/2 flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-[#E7EEF3] px-5 py-3"><div className="min-w-0"><div className="truncate text-sm font-extrabold text-[#193B57]">Xem trước chứng từ</div><div className="truncate text-[11px] text-[#71869A]">{item.file.name}</div></div><button type="button" onClick={onClose} className="drawer-close-action" aria-label="Đóng"><X size={18} /></button></div><div className="min-h-[420px] flex-1 overflow-auto bg-[#F4F7FB] p-4">{contentType === "application/pdf" ? <iframe title={item.file.name} src={objectUrl} className="h-[68vh] w-full rounded-lg border border-[#DDE7F0] bg-white" /> : contentType === "application/xml" ? <pre className="whitespace-pre-wrap break-words rounded-lg border border-[#DDE7F0] bg-white p-4 text-xs leading-5 text-[#193B57]">{xml || "Đang đọc XML..."}</pre> : <img src={objectUrl} alt={`Xem trước ${item.file.name}`} className="mx-auto max-h-[68vh] rounded-lg border border-[#DDE7F0] bg-white object-contain" />}</div></section></>; }
function DocumentIcon({ contentType }: { contentType: string }) { return contentType.includes("xml") ? <FileCode2 size={16} className="shrink-0 text-[#7554B8]" /> : contentType.startsWith("image/") ? <FileImage size={16} className="shrink-0 text-[#A86B00]" /> : <FileText size={16} className="shrink-0 text-[#2666A8]" />; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block space-y-1.5"><span className="text-xs font-extrabold text-[#526779]">{label}</span>{children}</label>; }
function SelectField({ label, children }: { label: string; children: React.ReactNode }) { return <div className="space-y-1.5"><span className="block text-xs font-extrabold text-[#526779]">{label}</span>{children}</div>; }
function Info({ label, value }: { label: string; value: string }) { return <div><dt className="text-[10px] font-extrabold uppercase tracking-[.08em] text-[#8AA0B6]">{label}</dt><dd className="mt-1 font-bold text-[#193B57]">{value}</dd></div>; }
function Metric({ label, value, icon, tone = "default" }: { label: string; value: number; icon: React.ReactNode; tone?: "default" | "teal" | "blue" }) { const colors = tone === "teal" ? "bg-[#E6F6F2] text-[#087A6A]" : tone === "blue" ? "bg-[#EAF3FF] text-[#2666A8]" : "bg-[#EAF0F7] text-[#193B57]"; return <div className="rounded-xl border border-[#DFE9F0] bg-white p-4 shadow-[0_8px_24px_rgba(16,42,67,.045)]"><span className={`grid h-9 w-9 place-items-center rounded-xl ${colors}`}>{icon}</span><div className="mt-4 text-[11px] font-semibold text-[#7890A5]">{label}</div><div className="mt-1 font-display text-2xl font-extrabold text-[#102A43]">{value}</div></div>; }
